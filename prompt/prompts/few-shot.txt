Convert the text below into a knowledge graph in RDF/Turtle.

Requirements:
- Return only RDF/Turtle. Do not include markdown or explanations.
- Represent the entities, concepts, and relationships stated in the text.
- Give every resource an `rdfs:label` using the wording from the text and a language tag.
- Use a known `wd:Q...` resource only when its QID is reliable. Otherwise, use a descriptive `kg:` resource. Never invent a QID.
- Use concise `kg:` predicates that preserve the meaning of the text.
- Every predicate must have an object, and the complete response must parse with `rdflib.Graph.parse(format="turtle")`.

Mandatory Turtle syntax rules:
- Follow RDF 1.1 Turtle syntax strictly.
- Every prefix used in a triple MUST be declared in the same response before its first use.
- If any `kg:` name is used, include exactly `@prefix kg: <https://example.org/wikidata-description/> .`.
- Never write two objects next to each other without Turtle punctuation.
- Use `,` only to separate multiple objects of the same predicate.
- Use `;` to continue the same subject with a different predicate, and write that predicate after `;`.
- Use `.` to end each subject statement.
- Before returning, verify that every used prefix is bound and every predicate-object list is correctly punctuated.

@prefix rdfs: <http://www.w3.org/2000/01/rdf-schema#> .
@prefix wd: <http://www.wikidata.org/entity/> .
@prefix kg: <https://example.org/wikidata-description/> .
@prefix xsd: <http://www.w3.org/2001/XMLSchema#> .

<EXAMPLES>

Text:
Alice manages a research laboratory in Lisbon.

RDF/Turtle:
@prefix rdfs: <http://www.w3.org/2000/01/rdf-schema#> .
@prefix wd: <http://www.wikidata.org/entity/> .
@prefix kg: <https://example.org/wikidata-description/> .

kg:alice rdfs:label "Alice"@en ;
  kg:manages kg:research_laboratory .

kg:research_laboratory rdfs:label "research laboratory"@en ;
  kg:located_in wd:Q597 .

wd:Q597 rdfs:label "Lisbon"@en .

Text:
A mango is a fruit.

RDF/Turtle:
@prefix rdfs: <http://www.w3.org/2000/01/rdf-schema#> .
@prefix wd: <http://www.wikidata.org/entity/> .
@prefix kg: <https://example.org/wikidata-description/> .

wd:Q169 rdfs:label "mango"@en ;
  kg:is wd:Q3314483 .

wd:Q3314483 rdfs:label "fruit"@en .

</EXAMPLES>

<CURRENT_TEXT>
Text:
${USER_TEXT}
</CURRENT_TEXT>

RDF/Turtle:
